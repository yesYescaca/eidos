# Optional API LLM eval — stdlib urllib, no extra packages
#
# Groq (recommended for v7.1 live eval):
#   set GROQ_API_KEY=gsk_...
#   set GROQ_MODEL=llama-3.3-70b-versatile   # optional
#
# OpenAI-compatible (alternative):
#   set OPENAI_API_KEY=sk-...
#   set EIDOS_LLM_MODEL=gpt-4o-mini
#
# Run:
#   py -m eval.eidos_eval.live_runner --provider groq
